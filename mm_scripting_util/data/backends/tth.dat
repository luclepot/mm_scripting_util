# standard backend file. 
# works for tth_CP process.

# backend name: an identifier, required
backend_name: tth_CP

# backend model: theory model, required
model: HC_UFO

# backend generation command: scattering stuff for madgraph, required
madgraph_generation_command: g g > x0 t t~, x0 > a a, t > l+ vl b, t~ > l- vl~ b~

# theory parameters are specified with 'parameter <name>: <*args>' syntax.  
# required args for theory parameters are:
# - lha_block : string
# - lha_id : int 
# - morphing_max_power : int
# - parameter_range : tuple
parameter ca: lha_block=frblock, lha_id=2, morphing_max_power=2, parameter_range=(0.,1.)

# parton-level observables are declared with 'observable <name>: <string>' syntax.
# these seem to be formed generally by indexing the p-vector, with some math
observable delta_ttbar: (p[2]+p[3]+p[4]).deltaeta(p[5]+p[6]+p[7])
observable x0_pt: (p[0]+p[1]).pt

# declare benchmarks:
benchmark w: ca=0.
benchmark sm: ca=1.